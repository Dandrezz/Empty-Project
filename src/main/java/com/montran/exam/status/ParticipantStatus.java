package com.montran.exam.status;

/**
 * This class of enum type, serves to identify the different states that an participant can have
 * @author Diego Portero
 *
 */
public enum ParticipantStatus {
	ACTIVE,
	REMOVED,
	DISABLE
}
